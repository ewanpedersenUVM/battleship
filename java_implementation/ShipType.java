/**
 * The ShipType enum represents the types of ships in the game Battleship.
 * Each ship type has a unique identifier.
 */
public enum ShipType {
  ST_AIRCRAFT_CARRIER,
  ST_BATTLESHIP,
  ST_CRUISER,
  ST_DESTROYER,
  ST_SUB;
}
